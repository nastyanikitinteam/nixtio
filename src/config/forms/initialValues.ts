import { FormValues } from '@/types';

// Initial values for forms

export const loginInitialValues: FormValues = {
  email: '',
  password: '',
};

export const contactInitialValues: FormValues = {
  name: '',
  email: '',
  message: '',
};

// Add more initial values as needed
